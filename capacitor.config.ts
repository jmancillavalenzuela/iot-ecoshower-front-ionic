import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'iot-ecoshower-setup',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
